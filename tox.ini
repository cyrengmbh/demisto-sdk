[tox]
envlist = py37,py38

[testenv]
passenv = *
deps = -r {toxinidir}/requirements.txt
       -r {toxinidir}/requirements-dev.txt
commands = demisto-sdk --version && pytest -p no:warnings -v {posargs}
